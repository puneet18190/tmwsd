<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <h3>Sign Up</h3>
  <p>Signing up for an account will allow to view a history of messages sent and received. Also, message control URLs will only be accessible after logging in. There may be some other benefits in the future...we'll see.</p>
  <%= f.error_notification %>
  <%= f.input :name, :autofocus => true %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation, :label => "Password (again)" %>
  <%= f.button :submit, "Sign up" %>
<% end %>

<%= render :partial => "devise/shared/links" %>
